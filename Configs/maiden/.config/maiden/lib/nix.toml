 [nix]
 about = "Nix related."

 [nix.build]
 bin = "nix"
 about = "Builds the default package"
 args = ["build"]

 [nix.install]
 sudo = true
 bin = "nix-env"
 about = "Install a package result"
 args = ["-i", "./result"]
